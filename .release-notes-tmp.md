- Fix: card stayed empty after Lovelace edits or view switches (Lit only fires `firstUpdated` once per element lifetime – the map is now rebuilt on re-attach and the history subscription re-established)
- Fix: history stream/fallback requests now use exactly the same parameters as the built-in map card
- Fit bounds re-runs after re-attach
